# IMU 水平与轴方向对齐后最终复测报告

## 结论

IMU 水平安装和 X/Y 方向调整成功。本轮室内复测中，左/右倾斜主要改变 `roll`，前倾/后仰主要改变 `pitch`，与代码的轴假设一致。静止和轻微颠簸场景中未再出现 `tilt_score>=0.5` 的长时间误触发。

**IMU 安装、连通、更新周期、主轴方向和室内误触发验收通过。**

但全部风险融合前验收尚有两项需要完成：在软件中扣除固定中性偏置，以及在安全室外条件下补测直线推行、普通转弯和停止。完成前继续保持 `pose: 0.00`。

## 测试条件

- IMU：WT61C，`115200 baud`。
- 端口：`/dev/serial/by-path/pci-0000:00:14.0-usb-0:6.2:1.0-port0`。
- 预检：`30/30 valid`。
- 场景：静止、左/右低幅度倾斜、前/后低幅度俯仰、左/右转动车头、轻微颠簸。
- 未测：直线推行、普通转弯、行进中停止和摔车。
- 代码提交：`3c65009301ecec19182d339cd1ec651f5f923a36`。

## 数据质量与同步

- 样本：`14,531/14,531 valid`，无效比例 `0%`。
- 解析包：加速度 `21,781`，角速度 `21,790`，角度 `21,794`。
- 坏校验帧 `0`，扫描丢弃字节 `0`。
- 更新间隔 P50/P95/P99：`10.057 / 10.068 / 10.077 ms`。
- 连续记录区间最大间隔：`11.094 ms`。
- 三类包主机侧同步偏差 P99：`0.017 ms`；最大 `10.131 ms`，通过 `200 ms` 候选上限。

## 静止零偏与重力

- `roll`：均值 `-5.296°`，标准差 `0.414°`，P95 `-5.004°`。
- `pitch`：均值 `-1.493°`，标准差 `0.104°`，P95 `-1.472°`。
- 加速度均值：`acc_x=+0.256`、`acc_y=-0.924`、`acc_z=+9.799 m/s²`。
- 三轴加速度合量：`9.845 m/s²`，接近标准重力，重力主要位于 `acc_z`。

`roll=-5.296°` 和 `pitch=-1.493°` 应作为当前最终安装的中性偏置，在计算风险特征前扣除，不建议通过继续搬动硬件追求绝对 `0°`。

## 车体动作与主轴

| 车体动作 | 主变化轴 | 相对前置静止的峰值 | 符号 |
|---|---:|---:|---|
| 左倾 | `roll` | `-18.428°` | 负 |
| 右倾 | `roll` | `+20.401°` | 正 |
| 前倾 | `pitch` | `+10.371°` | 正 |
| 后仰 | `pitch` | `-12.702°` | 负 |

验收结果：

- 左/右主变化轴均为 `roll`，符号相反：通过。
- 前/后主变化轴均为 `pitch`，符号相反：通过。
- 实测轴映射与代码假设一致：通过。

## 角度与角速度

- `roll` 变化率与 `gyro_x` 相关系数 `0.958`，同号比例 `92.0%`。
- `pitch` 变化率与 `gyro_y` 相关系数 `0.955`，同号比例 `82.9%`。

两组轴对的方向一致性通过。

## `tilt_score` 误触发

- 静止均值 `0.152`，最大值 `0.197`，`>=0.5` 占比 `0%`。
- 轻微颠簸最大值 `0.266`，`>=0.5` 占比 `0%`。

室内静止和轻微颠簸误触发验收通过。扣除中性偏置后，静止 `tilt_score` 还应进一步接近零。

## 后续行动

1. 将当前最终安装姿态固定，不再改变 IMU 位置和方向。
2. 为读取器增加中性姿态校准，计算 `body_roll=roll-roll0`、`body_pitch=pitch-pitch0`。
3. 使用扣除中性偏置后的角度重新计算 `tilt_score`。
4. 安全条件允许时补测直线推行、普通转弯和停止，并标定 `brake_score` / `bump_score` / `tilt_score`阈值。
5. 完成软件中性校准和户外补测前，继续保持 `pose: 0.00`。
