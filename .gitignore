# =============================================================================
# Intel-Cup-2026 .gitignore
# =============================================================================
# 原则：
# 1. 不上传大文件（数据集、模型权重、测试结果）
# 2. 保留必要代码和配置
# 3. 保留少量样例确保代码可运行
# 4. 拉取后需下载模型和数据才能完整运行
# =============================================================================

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.Python
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# OS
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# 敏感信息
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.yaml

# -----------------------------------------------------------------------------
# 运行时输出（测试结果、训练输出）
# -----------------------------------------------------------------------------
runs/
outputs/
logs/
*.log

# 数据集打包目录（不上传）
dataset_packages/

# -----------------------------------------------------------------------------
# 大文件和数据（不上传）
# -----------------------------------------------------------------------------

# 完整数据集
datasets/
# datasets/bdd100k/          # 100K 图片
# datasets/bdd100k_subset_500/  # 测试子集 500 张
# datasets/cityscapes/       # Cityscapes 数据集
# datasets/acdc/             # ACDC 恶劣天气数据集
# datasets/idd/              # IDD 数据集
# datasets/rider_self_collected/  # 自采数据

# 模型权重（已启用，会被提交到 Git）
# 注意：YOLO 模型约 6MB，clone 后可直接使用
# *.pt
# *.pth
# *.onnx
# *.xml
# *.bin
# *.engine

# 数据处理中间文件
data/raw/
data/processed/

# -----------------------------------------------------------------------------
# 样例目录（保留目录结构，上传少量样例）
# -----------------------------------------------------------------------------

# 样例图片目录 - 保留空目录结构
# 可以在拉取后放置少量测试图片
data/samples/
data/samples/detection/
data/samples/segmentation/
data/samples/images/

# 样例文件（如果需要）
# !data/samples/detection/.gitkeep
# !data/samples/images/sample.jpg

# -----------------------------------------------------------------------------
# 文档和配置文件（保留）
# -----------------------------------------------------------------------------

# 保留必要的目录结构
# data/splits/              # 数据划分文件
# data/dataset_configs/    # 数据集配置文件

# 排除规则：不要忽略配置和文档
!data/
!data/splits/
!data/splits/*.txt
!data/dataset_configs/
!data/dataset_configs/*.yaml

# -----------------------------------------------------------------------------
# 视频和音频文件（不上传）
# -----------------------------------------------------------------------------
*.mp4
*.avi
*.mov
*.mkv
*.wav
*.mp3
*.flac

# -----------------------------------------------------------------------------
# 临时文件
# -----------------------------------------------------------------------------
*.tmp
*.temp
.cache/
*.bak
*.swp
*.swo

# -----------------------------------------------------------------------------
# 压缩文件
# -----------------------------------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.rar
*.7z

# -----------------------------------------------------------------------------
# 其他
# -----------------------------------------------------------------------------
*.pptx
*.docx
*.xlsx
notes.txt
todo.txt
